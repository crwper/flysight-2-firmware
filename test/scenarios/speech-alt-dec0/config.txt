; A spoken-ALTITUDE entry (Sp_Mode 12) with Sp_Dec 0. For altitude, the step
; size is step_size = (units==meters ? 10000 : 3048) * Sp_Dec, so Sp_Dec 0
; makes step_size == 0 -- a divide-by-zero (QUIRKS #19). FS_Speech_BuildValue
; guards this with `if (decimals != 0)` and, on the FALSE leg, clears `valid`
; and emits nothing (B2 skip rule). So even above ALT_MIN (where the SpeechSource
; rotation DOES call the builder for a mode-12 entry) NOTHING is announced.
; Identical to speech-alt-only-low except Sp_Dec 100 -> 0.
; Characterizes / covers: audio_speech.c the mode-12 `decimals != 0` FALSE leg
; (QUIRKS #19 / #20 residue) -- reached only via a mode-12 Sp_Dec 0 config.

Model:     7
Rate:      200

Mode:      2
Min:       0
Max:       300
Limits:    1
Volume:    6

Mode_2:    9
Min_Val_2: 300
Max_Val_2: 1500
Min_Rate:  100
Max_Rate:  500

Sp_Rate:   3
Sp_Volume: 6

Sp_Mode:   12    ; altitude only
Sp_Units:  1
Sp_Dec:    0     ; step_size == 0 -> divide-by-zero guard FALSE leg -> skip (QUIRKS #19)

V_Thresh:  300
H_Thresh:  0

Use_SAS:   1

DZ_Elev:   0
Alt_Units: 1
