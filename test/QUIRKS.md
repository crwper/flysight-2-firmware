# Characterization quirks log

Behaviours of the current `audio_control.c` that were surprising when the
golden traces were first read. Each needs a decision before the rewrite:
**preserve** (the rewrite must reproduce it) or **fix** (the rewrite changes
it deliberately, with a reviewed golden update).

Status: `confirmed` = visible in a committed golden trace;
`suspected` = seen in code review, not yet pinned by a scenario.

| # | Status | Behaviour | Decision |
| --- | --- | --- | --- |
| 1 | confirmed (`alarms-freefall` t=147.6/185.2/222.6) | Altitude alarms fire on the **climb** as well as in freefall. The crossing test in `updateAlarms` (`alarm_elev >= min && alarm_elev < max`) is direction-agnostic and is not gated by `V_Thresh`/`H_Thresh` (unlike Alt_Step announcements, which are). | **PRESERVE** -- intended; competitors use this to check their alarm altitudes on the way up. |
| 2 | confirmed (`alt-step-feet` t=525.8) | `11.wav` is missing from the audio asset set, so any "eleven" (e.g. the 11,000 ft announcement) is silently skipped: PLAYFAIL, and the announcement plays as "...thousand feet" with no leading number. Asset gap, not a code bug, but competition-relevant. Same gap would affect the `'b'` (11 o'clock) speech token. | **ASSET FIX (PENDING)** -- not intended; Michael investigating the audio pack. Code behaviour (missing file plays nothing) is preserved. **B6 check 2026-07-06:** `TEMP/AUDIO/11.wav` still absent (10.wav and 12.wav present), so `alt-step-feet` was NOT re-blessed and this row stays open. When the `11.wav` asset lands, re-bless `alt-step-feet` (the 11,000 ft "eleven" flips PLAYFAIL->PLAY with a downstream timestamp cascade) and close this row. **C1 re-check 2026-07-06:** `TEMP/AUDIO/11.wav` still absent (10.wav / 12.wav present) -- row stays OPEN, final state **asset-pending**. This is the only non-closed row at C1 closeout. |
| 3 | confirmed (`init-speech` t=7.73) | The first-fix beep is deferred until the speech buffer is empty. With `Init_Mode: 1` the whole startup speech test (~7.7 s) plays before the fix beep sounds, even though the fix arrived at t=3. | **PRESERVE** -- intended; the first-fix beep is not time-critical, but init speech must play uninterrupted at power-on. |
| 4 | confirmed (`alt-step-feet` t=0.65-1.65) | With `Alt_Step > 0`, the say-altitude-on-first-fix feature announces the current altitude immediately on gaining a fix -- on the ground that is "zero feet". | **PRESERVE** -- intended; altitude mode announces ground elevation once vAcc < 10 m so users can verify `DZ_Elev` is set correctly (should say ~0). |
| 5 | confirmed (all traces) | The consumer timer period is 20 RTC ticks ~= 9.764 ms, not 10 ms (integer division in `CONSUMER_TIMER_TICKS`), so tone rates are ~2.4% faster than the `FS_CONFIG_RATE_*` constants suggest. Modelled exactly by the simulator. | **PRESERVE** -- keep the 20-tick period exactly as-is in the rewrite. **B1 note:** when the metrics went to SI float, the 16-bit tone-rate accumulator (`state.tone_timer`, the `0x10000` wrap) AND the rate VALUE feeding it were kept INTEGER and bit-for-bit UNCHANGED -- the tone `value_2`/rate path stays in the original integer arithmetic (`FS_FlightParams_GetRateValue`), because a float rate drifts the free-running phase and changes beep counts/timing. The 125 ms beep length is likewise unchanged. Only the in-band tone PITCH is now rounded from that same integer ratio (was truncated), so a pitch may differ by +/-1 Hz. |
| 6 | suspected | `updateTones`'s `static int32_t x0, x1, x2` (CHANGE_IN_VALUE_1 history) are function-scope statics that `FS_AudioControl_Init` never resets: the first tone rate after a re-init can use stale data from the previous session. | Analyzed 2026-07-04: first BOOT is safe (x0's INVALID initializer propagates through x1/x2 for two samples before the zero-initialized slots are compared). The gap is only DeInit/Init without power loss: stale history gives 1-2 samples of wrong rate, usually inaudible on the ground below thresholds. **FIX in rewrite** via state-struct reset; no hotfix. Cross-session behaviour not currently characterized (harness does one Init per run). **DONE (A1):** all mutable module state was consolidated into a single `static FS_AudioControl_State_t state;` (including the former `x0/x1/x2` change-history statics), and `FS_AudioControl_Init` does `memset(&state,0,...)` then re-seeds `x0 = INVALID_VALUE`, so a re-init no longer inherits stale history. |
| 7 | suspected | `speakValue` writes to its local *copy* of the config (`config->speech[i].decimals = 0` for direction modes); this only works because `producerTask` re-copies the config every cycle. A rewrite that passes the real config pointer would corrupt it. | **FIX in rewrite**: config is read-only in audio code; departures from config behaviour (like forcing decimals for direction modes) happen in locals. Behaviour-identical today because the producer re-copies each cycle. **DONE (A7):** const config end-to-end -- every audio-module helper takes `const FS_Config_Data_t *`, the producer snapshot is `const`, and the direction-mode `decimals = 0` override lives in a local inside `FS_Speech_BuildValue`. Verified zero writes to `config->`/`config.` in the module. |
| 8 | confirmed (`alarm-exact-crossing`) | Alarm crossing uses a half-open interval (`>= min && < max`). When the alarm elevation lands exactly on a sample: descending, it fires ON that sample (t=3.0); ascending, it fires one sample LATE (t=8.2, ~0.2 s / ~10 m past the elevation), because the exact-hit sample is excluded as the upper bound and included as the lower bound of the next pair. | **PRESERVE**, with the spec sentence: "the alarm fires on the first sample pair whose [low, high) altitude interval contains the alarm elevation." Guarantees exactly-once per crossing including reach-and-hold (fires at first touch from above; pinned by `alarm-hold-at-elev`). Known asymmetry: an exact touch from below without overshoot never fires -- accepted, measure-zero with real GNSS data. |
| 9 | confirmed (`config-malformed`) | In `config.c`, an `Alarm_Type`/`Alarm_File`/`Win_Bottom`/`Sp_Units`/`Sp_Dec` line appearing before the corresponding `Alarm_Elev`/`Win_Top`/`Sp_Mode` line writes to index `num_x - 1 = -1` (out-of-bounds). Config-parser scope, not audio_control, but discovered here. | **DONE (B7)** — FIXED SCOPE is exactly the *member-before-opener index -1 write*. Each of the five group-member guards now requires `config.num_x > 0` (an opener has been seen) so a leading member is IGNORED, not written to index -1. The three groups (alarms / silence windows / speech) are the only repeated-group keys in the parser; all audited. The `<= FS_CONFIG_MAX_x` upper bound is CORRECT and LOAD-BEARING and was deliberately KEPT (the card's `<= MAX -> <` suggestion was WRONG and was NOT applied): the opener's `< MAX` pre-increment guard caps `num_x` at MAX, so a legitimately *full* group has `num_x == MAX` and its last member must write index `MAX-1` — `<` would drop that member and CHANGE existing goldens (`silence-window-metric` defines 2 windows = MAX_WINDOWS and relies on window 2's `Win_Bottom`; `speech-alt-mode`/`-multi-a`/`-multi-b`/`-nav`/`-invalid-cfg` define 3 speech = MAX_SPEECH and rely on the 3rd entry's `Sp_Units`/`Sp_Dec`). `config-malformed` pins the fix: 3 stray members lead the file yet the well-formed `Alarm_Elev 1500`/`Alarm_Type 1` still fires exactly once (no phantom/-1 alarm). All existing goldens byte-identical (well-formed configs -> tightening is a no-op). A DISTINCT residual (member of an *overflow* opener, still in-bounds) is NOT fixed here — see #21. |
| 10 | confirmed (`silence-window-metric`) | Whether an Alt_Step announcement at a silence-window **edge** is suppressed depends on approach direction: suppression tests the crossing *sample*, which lands just inside the window from one direction and just outside it from the other. Climbing, the 3000 m step (window top) announced and 2500 m (bottom) was suppressed; descending, the mirror image. | **REWRITER'S CHOICE**: keep whichever interval convention yields the simplest code; a 1 mm shift at a window edge is indistinguishable to the user. Exact-edge golden diffs acceptable with review. **DONE:** the rewrite kept the original crossing/window interval convention verbatim, so no deliberate edge-convention change was made and `silence-window-metric` continued to pin the same direction-dependent edge behaviour through the rewrite. Settled -- no outstanding choice. |
| 11 | confirmed (`silence-window-metric` t=228-232) | Alt_Step announcements fire on the **climb** as well as descent -- like alarms (#1) the step-crossing is direction-agnostic, but unlike alarms it IS gated by `V_Thresh`/`H_Thresh`, so climb announcements only occur when the climb rate exceeds `V_Thresh` (here 8 m/s > 3 m/s). In `alt-step-feet` (V_Thresh 10 m/s) the climb was silent. | **PRESERVE** -- altitude announcements are "background" speech and behave like other speech; alarms are special because the user requested that specific altitude. |
| 12 | confirmed (`speech-zero-div`) | When a speech value is empty (glide with velD == 0, inverse glide with gSpeed == 0), what plays depends on `Sp_Dec` through buffer arithmetic: Sp_Dec 2 -> the label plays alone ("glide" ... silence); Sp_Dec 1 -> **nothing at all** (the truncation step writes the terminator over the label byte); Sp_Dec 0 -> label alone. | **DONE (B2)**: a value that cannot be computed (division guard) skips its utterance entirely -- no label, no value, no suffix (`FS_Speech_BuildValue` clears `valid` and returns an empty queue). `speech-zero-div` re-blessed: the periodic label-only `glide.wav` utterances vanish, leaving only the first-fix beep. |
| 13 | confirmed (`speech-nav`, `speech-nav-gated`) | **BUG: uninitialized read.** In `speakValue`, `tVal` is only assigned inside the End_Nav/Max_Dist-gated blocks of modes 5/7, but step 3 always reads it to decide the left/right suffix (lines 584-585). When the gate skips the calculation, a spurious "left" or "right" is spoken based on stack garbage (e.g. during climb below End_Nav). **Corruption mechanism (found A3, fuzz seed 372):** when gated, the `decimals = 0` override never runs (it lives inside the gated block), so the garbage suffix and its `'\0'` terminator land at Sp_Dec-dependent offsets in the backward-built buffer and can OVERWRITE the label region — not merely append a spurious l/r. So the old code voices a garbage `right.wav` IN PLACE OF the label, shifting the whole utterance. | **DONE (B2)**: the direction-suffix decision now runs only on the valid path, where `dir_val` was actually computed, so the uninitialized read is eliminated STRUCTURALLY (one rule with #12: gated/uncomputable -> skip the whole utterance). `speech-nav` (4 gated label-only directn/bearing below the 37.856 m End_Nav gate) and `speech-nav-gated` (84 Max_Dist-gated mode-5 directn utterances) re-blessed. (A3 had already dropped the garbage suffix for non-golden inputs; B2 removes the remaining gated utterances from the goldens themselves.) **Side effect, ratified (Michael 2026-07-06):** removing gated utterances unmasks the tone that was previously held during garbage playback — `tone_hold` exists only for audio that actually plays, and nav gating was never meant to gate tones (the tone path has its own End_Nav gate). `speech-nav-gated` gains +66 min-pitch (f0=220) beeps in the freed windows. |
| 14 | confirmed (coverage + grep) | **Dead code:** the `'o'`/`'a'`/`'b'`/`'c'` speech tokens in `consumerTask` (oclock/10/11/12.wav, lines ~1025-1039) have no writer anywhere in the codebase -- FlySight 1 leftover. Unreachable from config + track. | **DONE (B6).** Verified dropped by A3 and confirmed clean: greps of `audio_speech.c`/`audio_control.c` find ZERO `oclock` and ZERO `'o'/'a'/'b'/'c'` char-route remnants; the only `*.wav` string literals are the token->filename table, where `10.wav`/`11.wav`/`12.wav` are the legitimate `TOK_NUM_10`/`TOK_NUM_11`/`TOK_NUM_12` entries (the old `'a'`/`'b'`/`'c'` 10/11/12-o'clock routes are gone). `11.wav` remains reachable via the teens token (`TOK_NUM_11 = "11.wav"`, required for "eleven"). No golden references `oclock.wav`. Clarified: `11.wav` itself IS required -- "eleven" (e.g. 11,000 ft) reaches it via the teens token, which is the PLAYFAIL in #2. Only the `'b'` token *route* was dead. |
| 15 | confirmed (coverage + config.c validation) | **Dead code:** `FS_CONFIG_MODE_LEFT_RIGHT` (mode 10, getValues lines 321-348) is unreachable: config.c accepts Mode 0-7 or 11, and Mode_2 0-9 or 11, only. | **KEEP the code** -- mode 10 stays reserved; may be enabled via config on develop (where nav modes are documented) later. No rush to remove. **PRESERVED (confirmed C1):** the `FS_CONFIG_MODE_LEFT_RIGHT` metrics case survives in `flight_params.c` (`FS_FlightParams_GetRateValue/GetValue`) and is unreached by any parser-valid config, so it is the allowed coverage residue (a): branch 108's case-10 arm + the case body branches 205/208/213/215 (lines 203-230). Re-confirmed exempt at C1; see row #20. |
| 16 | confirmed (`tone-vert-bearing-rate`, `tone-bearing-north`) | **BUG: heading scale.** The Mode_2 == 7 recalculation in `updateTones` (line 773) passes `current->heading` to `calcRelBearing` RAW (deg * 1e5); every other caller divides by 100000. Result: for any nonzero heading the relative bearing is hugely negative, `180 - ABS(...)` is hugely negative, and the rate pegs at MINIMUM regardless of heading; at heading exactly 000.00000 deg it flips to MAXIMUM. Mode_2 7 with Mode != 7 is functionally broken on the device. (Mode == Mode_2 == 7 is fine -- it reuses value 1, which is scaled correctly.) | **DONE (B3)**: the `updateTones` Mode_2==7 recalc now divides `current->heading` by 100000 before `calcRelBearing`, matching every other caller (flight_params.c `GetRateValue`/`GetValue`, audio_speech.c). The division is an INTEGER truncation on the raw int32 GNSS field, preserving the QUIRKS #5 integer rate enclave (B1 kept this path integer, which is why the bug survived B1 for B3 to fix). `tone-vert-bearing-rate` re-blessed: the rate now sweeps with the 3 deg/s canopy spiral (fastest ~0.20 s/beep when heading == bearing 270, slowest ~0.9 s/beep when heading == 90 opposite), instead of pinned at Min_Rate throughout; +303 net beeps, pitch (value_1) and the pre-canopy heading==0 region byte-identical. `tone-bearing-north` verified BYTE-IDENTICAL and NOT re-blessed: its due-north track has velE==0 so heading is exactly 000.00000 deg everywhere -- raw and /100000 headings both equal 0, so max-rate behaviour is preserved unchanged (the fix only diverges for nonzero heading). |
| 17 | confirmed (`alt-vacc-never`: zero PLAY events in a full jump) | If vAcc never drops below 10 m, FLAG_SAY_ALTITUDE never clears, and because both the Alt_Step announcements and ALL periodic speech are gated on that flag, a persistently poor vertical accuracy silently disables every announcement and all speech for the whole jump (tones still work). | Discussed 2026-07-04. Intent: user must hear the ground-elevation confirmation before any altitude indication (wrong-DZ protection). **DECIDED 2026-07-04**: suppress only ALTITUDE indications (Alt_Step announcements, Sp_Mode 12 entries) until the ground-elevation announcement has played; non-altitude speech unaffected. Alarms fire unconditionally on fix, regardless of vAcc or announcement state (an alarm silenced by a pessimistic accuracy estimate is the worse failure). **DONE (B4)**: the `say_altitude` latch now gates ONLY altitude indications. `SpeechSource_MaybeSpeak` no longer blocks on the latch as a whole; instead a Sp_Mode-12 (ALTITUDE) rotation entry is SKIPPED while the latch is set (rotation advances, exactly like the ALT_MIN floor skip -- not blocking), so non-altitude entries keep their cadence. The Alt_Step announcement gate (`!say_altitude`, `AltModeSource_Update`) is unchanged. Alarms were already unconditional. `alt-vacc-never` re-blessed: it now voices periodic vertical-speed (mode-1) speech throughout the jump (35 PLAY events, previously zero); the ground-elevation announcement and all step announcements remain absent (vAcc never good), first-fix beep unchanged, and each new utterance holds the tone so 27 tone beeps disappear inside the utterance windows (the ratified B2/#13 tone_hold side effect -- every removed beep verified to align with an added utterance). New `alt-gate-mixed` scenario pins the split: mode-1 speech plays during the poor-vAcc window while the aircraft is already above ALT_MIN, and mode-12 speech + Alt_Step announcements + the ground-elevation announcement all begin only after vAcc recovers (~t=70). `alt-vacc-gate` byte-identical (its announcement precedes everything). |
| 18 | confirmed (`alarm-type-none`) | An alarm with `Alarm_Type: 0` plays nothing when crossed but STILL cancels queued speech (`*speech_ptr = '\0'` runs after the type switch), audibly clipping the speech cadence. | **DONE (B5)**: Alarm_Type 0 entries are filtered out of the alarm list at `FS_AudioControl_Init` (into `AlarmSource_t state.alarm`), so a type-0 alarm is ignored completely -- no sound, no speech cancel, no suppression window. The suppression-window scan, the crossing scan, `Arbiter_FireAlarm`, and the producer's "did any alarm fire" collision count all index the SAME filtered list, so dropping a type-0 entry can neither shift an index nor miscount. `alarm-type-none` re-blessed: the vertical-speed speech no longer clips at the 2000 m crossing (the type-0 alarm's speech-cancel is gone) -- everything above the crossing byte-identical, the speech stream re-phases from t=553.589 and gains one token (the previously-clipped utterance now completes), END unchanged. The card's amended prediction of tone-beep shifts there did NOT materialize: continuous back-to-back speech already holds the tone off in BOTH versions (the only BEEP is the byte-identical first-fix beep) and Win_Above/Below are 0, so there is no suppression-window or tone_hold change to observe -- expected-list satisfied, no gap. New scenario `alarm-cancels-speech` pins the CONTRAST (a REAL type-1 alarm at 2500 m with Win 0 still cancels queued speech mid-utterance -- the beep cuts `9.wav` at t=843.6 and the queued remainder is dropped), and is the kill scenario for mutation M33 (drop the crossing's `FS_Speech_Clear`). |
| 19 | confirmed (fuzz_diff seed 12; repro: Sp_Mode 12 + Sp_Dec 0) | **BUG: division by zero.** `speakValue` mode 12 computes `step_size = 10000 (or 3048) * decimals`; `Sp_Dec: 0` makes it 0 and the next line divides by it. Host sim crashes (0xC0000094); on the Cortex-M4, SDIV by zero yields 0 (unless DIV_0_TRP is set), so the device likely announces "zero" instead — UB papered over by ARM semantics, and a host/target divergence the sim cannot model. | **DONE (B2) for the crash; clamp still B7.** The B2 skip rule now guards mode 12: `Sp_Dec 0` makes `step_size == 0`, so `FS_Speech_BuildValue` clears `valid` and emits nothing instead of dividing by zero (no host crash, no target UB). A defensive config.c clamp (Sp_Dec >= 1 for mode 12) is still deferred to B7. No committed golden exercises Sp_Mode 12 + Sp_Dec 0, so no golden changed for this row. **B7 decision: DECLINED the clamp (deferred, not implemented).** B2 already fixes the crash at runtime (mode-12 `step_size == 0` -> skip). Clamping at parse time would (a) be a behaviour CHANGE for a mode-12 Sp_Dec-0 entry — it would announce (Sp_Dec forced to 1) instead of skipping — and (b) make the `audio_speech.c` `decimals != 0` guard's FALSE leg permanently unreachable, erasing the QUIRKS #20 coverage item (the mode-12 Sp_Dec-0 residue). Since the crash is already handled and the clamp only removes a coverage target while changing behaviour, no clamp was added. (It changes no existing golden either way — none has mode-12 Sp_Dec-0 — but the coverage interaction is the deciding factor.) |
| 20 | test/coverage note (A7 closeout) | **Phase A branch-coverage residue.** After the A7 milestone (filter `.*audio_control\|.*flight_params\|.*audio_speech`, `--txt-metric branch`) three items are uncovered and were RATIFIED by Michael 2026-07-05 as allowed residue: (a) the reserved LEFT_RIGHT/mode-10 metrics entry (flight_params.c ~85/182/185/190/192, QUIRKS #15); (b) `FS_FlightParams_GetSASCorrectionFactor` — develop-branch-merge scaffolding, intentionally unused on this branch (BRIEF §3), exempt from the 100% rule; (c) two REACHABLE-but-golden-unexercised branches that are required behaviour the frozen suite never hits: `audio_speech.c:131` (MINUS token for a negative speech value — vertical-speed/glide/dive while climbing, velD<0) and `audio_control.c:771` (Alt_Step crossing skipped because speech is already queued). | **COVER in Phase B**: B2 should add/extend a speech scenario with a climbing negative value (covers audio_speech.c:131 MINUS); B4's `alt-gate-mixed` (Alt_Step + speech) should be checked to cover audio_control.c:771 (alt-step-while-speech-pending). Re-run the A7 coverage command in C1 and confirm residue is (a)+(b) only. **B4 update**: `alt-gate-mixed` COVERS the alt-step-while-speech-pending branch -- audio_control.c is now 100% branch (189/189), the `else if (want_alt_step && !FS_Speech_HasPending(...))` FALSE leg is hit by the frequent freefall step crossings while a periodic utterance is queued. As a bonus the same scenario voices a climbing vertical speed ("minus.wav"), covering item (c)'s MINUS branch too. Remaining residue in the widened filter is now in audio_speech.c and flight_params.c (LEFT_RIGHT/GetSAS, items (a)+(b)); C1 to re-confirm. **[orchestrator 2026-07-06] Precise audio_speech.c residue after B4: 3 branches — :372 (the QUIRKS #19 `decimals != 0` guard FALSE leg; reachable only via a mode-12 Sp_Dec 0 entry, which no golden has) and :86 / :423 (unexercised `labelToken`/Step-3-suffix `switch` cases for label/unit modes not present in any scenario config). MINUS (:133) and audio_control:771 are now COVERED. C1 should either add a mode-12 Sp_Dec-0 scenario + configs exercising the missing label/unit modes, or justify these as reachable-but-unexercised residue.** **C1 FINAL -> DONE (2026-07-06):** re-ran the widened coverage command (fresh gcda, 55 scenarios). Outcome: **audio_control.c 100% line + 100% branch; audio_speech.c and flight_params.c 100% of REACHABLE line+branch.** Closures: (1) audio_control.c:663 -- a NEW uncovered branch (the alarm-type `switch`'s no-match arm in `Arbiter_FireAlarm`), which appeared when B5's `type != 0` init filter removed its former type-0 exerciser -- is COVERED by new scenario `alarm-type-unknown`. `Alarm_Type 5` is parser-valid (config.c does NOT clamp type), passes the `!=0` filter, and its crossing fires the switch's no-match arm (plays nothing, still cancels queued speech). (2) The mode-12 `decimals != 0` FALSE leg (#19 skip) is COVERED by new scenario `speech-alt-dec0` (a `Sp_Mode 12` / `Sp_Dec 0` entry that reaches the builder above ALT_MIN and skips). REMAINING residue is exactly four branches, all documented: (a) LEFT_RIGHT/mode-10 metrics entry (flight_params, #15) and (b) `FS_FlightParams_GetSASCorrectionFactor` (#20b) -- the two ratified-exempt items; PLUS two audio_speech.c switch **no-match/default arms** -- `labelToken`'s `default` (:86 / line 98) and the Step-3-suffix `switch`'s implicit no-match (:423). Those two are structurally UNREACHABLE: `FS_Speech_BuildValue`'s value switch only sets `valid` for modes {0-7,11,12} and returns early for anything else, and those exact modes are all explicit cases downstream, so no parser-valid config reaches the default arm (same dead-switch-arm class as the flight_params no-match; the frozen module can't have the arms deleted). Justified per the C1 card's "document genuinely-unreachable branches as residue" clause rather than covered. |
| 21 | confirmed (`config-malformed`) | **config.c overflow-rebind (distinct from #9's -1 write, which is fixed).** With MORE than MAX openers of a repeated group (>2 `Win_Top`, >3 `Sp_Mode`, >20 `Alarm_Elev`), the overflow opener is DROPPED by the opener's `< MAX` guard (`num_x` caps at MAX), but the overflow entry's following member keys still write `x[num_x - 1] == x[MAX-1]` — rebinding the LAST VALID entry's field to the overflow value. This is IN-BOUNDS (not the -1 bug) — a wrong value, only reachable with a malformed/overflowing user config. A numeric guard cannot fix it: a legitimately full group and an overflowing one both have `num_x == MAX`, so `x[MAX-1]` is a valid write for the real MAXth entry and an erroneous rebind for the dropped overflow entry, indistinguishable from `num_x` alone. A proper fix needs an opener-side "group full / current-group-invalid" flag set when an opener is dropped, which members would then honor. | **ACCEPTED RESIDUAL (Michael 2026-07-06)** — reviewed with the B7 `<= MAX` analysis and accepted; config.c left unchanged (the `<= MAX` member guards are correct and load-bearing, see #9). PINNED by `config-malformed` (B7 follow-up): a 3rd `Win_Top` overflows MAX_WINDOWS=2 (dropped, `num_windows` stays 2) and the following `Win_Bottom 600` rebinds `windows[1].bottom` from 1000 to 600, extending window 2 to [600,1200]; the trace shows the vertical-speed tone SUPPRESSED down to ~600 m (no beeps in the 600..1000 m band, which would sound if the member had been ignored). A proper opener-side-flag fix is out of the audio-rewrite plan's scope; revisit only if malformed-config robustness is ever warranted. |
